## What it does
It takes in a string input of a lower case and outputs the string as uppercase.

## How to run 
Compile with: g++ filename.cpp -o filename
Run with: ./filename

## What I learned
there are different ways one can manipulate the input of a user to suit their required needs for a particular task at hand.